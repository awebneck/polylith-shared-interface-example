(ns shared-int.example.do-thing-two.interface-test
  (:require [clojure.test :as test :refer :all]
            [shared-int.example.do-thing-two.interface :as do-thing-two]))

(deftest dummy-test
  (is (= 1 1)))
